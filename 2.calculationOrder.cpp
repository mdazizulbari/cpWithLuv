#include <bits/stdc++.h>
using namespace std;

int main()
{
	// double > float > long long int > long int > int > characters
	double a = 3/2;
	// calculation is being first than it's being stored in the duble
	cout << a;
	return 0;
}